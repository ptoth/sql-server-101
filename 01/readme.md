## ✅ **1. alkalom – Alapok áttekintése + összetettebb SELECT-ek**

### 🔹 Téma: Ismétlés + haladó lekérdezések

#### A) **Rövid ismétlés**

* Adatbázis fogalma
* Tábla létrehozás (CREATE TABLE)
* Alap SQL parancsok: `SELECT`, `INSERT`, `UPDATE`, `DELETE`
* Szűrés (`WHERE`, `BETWEEN`, `LIKE`, `IN`)
* `ORDER BY`, `TOP`

#### B) **Haladó SELECT használata**

* `DISTINCT`, `ALIAS` (`AS`)
* Összetett feltételek (`AND`, `OR`, `NOT`)
* Beágyazott SELECT-ek (subquery)
* `CASE` szerkezet

#### C) **Csoportosítás (GROUP BY, HAVING)**

* Aggregáló függvények: `COUNT`, `SUM`, `AVG`, `MIN`, `MAX`
* Csoportosítás: `GROUP BY`
* Szűrés aggregált értékekre: `HAVING`

#### Gyakorlat:


---
